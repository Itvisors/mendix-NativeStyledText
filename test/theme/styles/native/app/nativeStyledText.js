import { brand, font } from "./custom-variables";

export const nextAppointmentStyledText = {
    container: {},
    defaultTextStyle: {
        color: font.color,
        fontSize: font.size
    },
    nameStyle: {
        color: brand.success
    },
    appointmentTime: {
        color: brand.primary
    }
};
